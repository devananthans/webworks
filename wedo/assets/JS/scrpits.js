
var typed = new Typed('.auto-type',
 {
  strings: ["Energy-Efficient", "Technology Enabled ", "Eco-Friendly"],
  typeSpeed: 200,
  backSpeed: 200,
  loop: true
});





// Initialization for ES Users
import { Ripple, initMDB } from "mdb-ui-kit";

initMDB({ Ripple });

// Initialization for ES Users
import { Carousel, initMDB } from "mdb-ui-kit";

initMDB({ Carousel });